let hello = "Hello"
let world = "world"
let epicPhrase = hello + ' ' + world + '!'
console.log(epicPhrase)

// template literal

let epicPhrase2 = `${hello} ${world}!` 

// multi-line strings

let lorem = 'esto es un estring \n' + 'esto es otro estring'

let lorem = ` esto es una frase epica 
esto es la continuacion de esa frase epica`